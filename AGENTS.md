# Lydia 外贸系统协作规则

## 产品目标

优先帮助外贸人员把已有询盘变成可核查、可排序、可执行的客户开发队列。任何新增功能都应回答：它是否让用户更快识别值得跟进的客户，或者提高建立信任和成交的概率。

## 数据与合规

- 仓库、测试和截图只能使用虚构的 Lydia 示例数据；域名优先使用 `example.com`。
- 不提交客户名单、真实联系方式、登录态、Cookie、API Key、私人关系数据或抓取快照。
- 每一条企业或联系人结论必须保留来源、观察时间、置信度和状态。
- 候选、推断、已验证三个状态必须清楚区分。
- 不实现自动群发、绕过平台限制、冒充熟人、未经授权的私域抓取或验证码规避。
- 不使用姓名、国籍、性别等敏感或无关属性作为客户质量分。

## 工程要求

- 先在 `packages/lead-core` 固化数据契约，再接入具体渠道。
- 外部数据源通过 `integrations` 适配，不进入核心评分逻辑。
- 评分必须可解释、可测试；固定权重要用真实成交结果再校准。
- 默认本地运行和人工确认，所有联网、外发与写回操作均需显式启用。
- 修改后运行 `npm run check`。

## 第三方代码

- 引入前记录仓库地址、版本 SHA、许可证、用途和改造范围。
- 无许可证项目只可研究思想，不复制代码、素材或文案。
- 保留许可证强制要求的版权和归属信息；这不等于把第三方用户或客户数据带进产品。
- GPL/AGPL 或来源不明的项目不得直接进入默认产品，除非 Lydia 明确接受对应义务。
