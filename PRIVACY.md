# 隐私说明

## 默认本地处理

插件默认使用本地规则识别场景、使用本地模板生成回复。客户资料、消息、回复历史和设置保存在 Chrome 的本地扩展存储中，不会由本项目主动上传。

## 可选 AI 服务

AI 功能默认关闭。用户主动开启并配置兼容 OpenAI 格式的接口后，只有在用户点击“生成回复”时，当前客户消息、选中的业务背景以及回复要求才会发送到该接口。数据如何被服务提供方处理，取决于用户选择的服务及其隐私政策。

插件只在首次调用某个接口域名时申请对应网络权限，不申请永久访问所有网页内容的权限。

## API Key

API Key 保存在 Chrome 本机扩展存储中。导出客户数据和设置时，插件会自动移除 API Key。用户可以随时在设置中清空它。

## 用户控制

用户可以导出自己的非敏感数据、导入备份、删除单条历史或在 Chrome 中卸载插件以移除本地数据。
