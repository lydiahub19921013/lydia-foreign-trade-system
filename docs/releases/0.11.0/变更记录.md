# v0.11.0 变更记录

- Lead schema 升级到 4，新增 `development.qualificationBaseline` 和最多 500 条追加式 `development.events`。
- 新增开发阶段推导、独立事件里程碑、开放/逾期待办、活动撤回和成交/流失后显式重新开发。
- 新增按首次分级统计的开发漏斗；确认归入主账户的次记录不重复计算。
- 工作台新增开发状态筛选、结果面板、基线转化表、开发记录对话框、客户时间线和完成待办操作。
- 命令行分级输出同步冻结基线并生成 `developmentSummary`。
- schema 4 开发记录导出、JSON 往返和沟通插件兼容纳入跨模块回归测试。
- 固定 Atomic CRM 与 Krayin CRM 的研究提交和 MIT 许可证；只吸收活动/任务/阶段分离原则，没有复制运行代码。

完整历史见仓库根目录 `CHANGELOG.md`。
