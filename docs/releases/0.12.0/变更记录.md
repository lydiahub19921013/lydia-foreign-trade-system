# v0.12.0 变更记录

- 新增无依赖、版本化的 IndexedDB 工作台快照模块。
- 新增串行自动保存协调，快速连续变更不会让旧快照覆盖新状态。
- 新增客户、开发、公开候选、企业/官网、候选邮箱、信任路径和必要界面状态恢复。
- 新增双白名单快照边界，联网授权勾选和未知字段不会持久化。
- 新增保存中、已保存、已恢复和失败状态，以及本机未加密与 JSON 备份说明。
- 新增带二次确认的清除入口并完成浏览器清除回归。
- 新增快照版本、异常/未来版本、保存/读取/清除和静态服务自动测试。

完整历史见仓库根目录 `CHANGELOG.md`。
