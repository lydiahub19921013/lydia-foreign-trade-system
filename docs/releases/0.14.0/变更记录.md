# v0.14.0 变更记录

- 新增版本化 `lydia-workspace-backup` 完整空间备份格式。
- 新增完整备份导出、25 MB 本机文件读取和恢复入口。
- 新增只创建新空间的不覆盖恢复；同名空间使用可解释的恢复后缀。
- 新增备份格式、空间标记和快照的逐层规范化与未来版本拒绝。
- 新增持久化对象的递归敏感命名字段剔除和循环引用报错。
- 新增虚构完整备份样例、服务器可达性测试、敏感字段测试和 IndexedDB 恢复往返测试。
- 同步客户空间、本机保存、架构、路线图和 Lydia Skill 的备份操作规则。

完整历史见仓库根目录 `CHANGELOG.md`。
