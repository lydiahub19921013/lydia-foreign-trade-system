# v0.16.0 验收

## 已验证

- `npm run check` 通过：核心、集成、工作台、扩展和可复现打包检查全部通过。
- `GET /documents` 返回 200；核心单证模块可被本机工作台加载。
- `POST /documents` 返回 405，单证页面没有服务端写入端点。
- 完整交易样例生成六种草稿，金额、箱数与重量在对应文档中保持一致。

## 使用前人工闸门

每份草稿都必须由业务人员核对客户主体、产品规格、价格、付款、唛头、HS 编码、税务、清关、银行和合同条款；本软件不替代法律、税务、银行或报关审核。
