# v0.2.0 变更记录

- 新增 Lydia 本地询盘工作台。
- 新增阿里巴巴/中国制造网常见中英文字段映射。
- 新增重复线索候选提示，禁止自动合并。
- 工作台导出改为稳定的 `lydia-qualified-leads` 数据契约，且不保存本机绝对输入路径。
- 外贸开发插件升级至 v0.2.0，可导入分级结果并保留旧客户、人工备注、邮箱和 WhatsApp。
- 插件构建改为确定性 ZIP，同一源码重复构建产生相同 SHA-256。

完整历史见仓库根目录 `CHANGELOG.md` 和 `apps/communication-extension/CHANGELOG.md`。
