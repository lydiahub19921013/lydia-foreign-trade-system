# v0.3.0 变更记录

- 在本地工作台增加 GLEIF 官方 LEI 企业身份查询。
- 将法定名称、登记编号、实体/LEI 状态、辖区、地址和更新时间转换为 Lydia Evidence。
- 只有 `ISSUED` 且 `FULLY_CORROBORATED` 的记录标记为已核实；其他状态保留为待核查。
- 增加自有通讯录/CRM 关系 CSV/JSON 导入、可解释排序和结果导出。
- 排除 `declined` 路径；无证据路径最高 49 分；任何介绍都需要关系人决定。
- 增加隐私披露、固定上游域名、超时与安全错误响应。

完整历史见仓库根目录 `CHANGELOG.md`。
