# v0.5.0 变更记录

- 增加联系人英文名与企业职能邮箱的有限组合，最多输出 10 个。
- 增加 MX、空 MX、A/AAAA 回退、域名不存在和 DNS 不确定状态。
- 只将 MX 作为 `mail-domain` 证据；任何具体 `business-email` 仍为候选或不确定。
- 当前官网页面公开列出相同地址时记录来源，但不升级为可投递邮箱。
- 候选邮箱加入询盘前校验企业域名；不一致时强制阻止，询盘无域名时只填补空字段。
- 不执行 SMTP、不发送测试邮件或营销消息。

完整历史见仓库根目录 `CHANGELOG.md`。
