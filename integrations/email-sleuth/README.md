# email-sleuth 可选适配器

此目录不包含也不会自动安装 `email-sleuth`。它只把使用者另行运行该工具后得到的结果转换为 Lydia Evidence。

启用前应确认：

- 遵守上游 MIT 许可证；
- 运行环境允许相应 DNS/SMTP 检查；
- catch-all、超时和端口受限均视为 `inconclusive`；
- 不对未同意营销的人自动发送测试邮件；
- 不把候选邮箱作为个人信息产品出售。

上游：https://github.com/buyukakyuz/email-sleuth
